# URL base del API Laravel (sin barra final). Misma IP/puerto que artisan serve.
VITE_API_URL=http://172.20.10.14:8000/api

# URL pública del front para og:image y enlaces canónicos (sin barra final)
VITE_SITE_URL=http://localhost:8080
