# Copia a .env y ajusta. Este archivo SÍ se puede commitear.
# NUNCA pongas aquí NVIDIA_API_KEY, GEMINI, OPENAI, DB ni MAIL: van en WorkConnect/.env (Laravel).

# Misma IP/puerto que: php artisan serve --host=... --port=8000
VITE_API_URL=http://127.0.0.1:8000/api

# URL pública del front (SEO, QR, redes)
VITE_SITE_URL=http://localhost:8080
